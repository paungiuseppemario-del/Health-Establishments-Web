# Testing

## Test Plan
TODO: Describe any manual and automated (unit) tests. Uniquely identify each test case. Include prerequisites and test data.

Manual Tests
Open home.html → page loads correctly
Navigate between pages → links work properly
Open map.html → map and locations display correctly
Submit contact form → form works and shows confirmation
Automated (Unit) Tests
Load data function → returns correct data
Filter function → returns correct results
Map function → displays correct markers
Error handling → handles missing/invalid data properly

Prerequisites: Website runs in a browser, required data files exist
Test Data: Sample location and map data

TODO: For each test described above, indicate the current status. 
| Use-Case ID | Requirement ID | Test Case                            | Status |
| ----------- | -------------- | ------------------------------------ | ------ |
| UC-01       | R-01           | Home page loads correctly            | Passed |
| UC-02       | R-02           | Navigation between pages works       | Passed |
| UC-03       | R-03           | Map displays locations correctly     | Passed |
| UC-04       | R-04           | Contact form submission works        | Passed |
| UC-05       | R-05           | Data loads correctly                 | Passed |
| UC-06       | R-06           | Filter function returns correct data | Passed |
| UC-07       | R-07           | Map markers display correctly        | Passed |
| UC-08       | R-08           | Error handling works properly        | Passed |
TODO: Add rows for each test, current status is eg. pass/fail
